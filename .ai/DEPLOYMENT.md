# DESPLIEGUE — icaco

> Mapa LOCAL → BUILD → DEPLOY → SERVICIO → DOMINIO → DEPENDENCIAS, por componente. Se documenta ANTES del primer deploy real, no se reconstruye después por auditoría.

| Componente | Local (dev) | Build | Deploy | Servicio/dominio | Dependencias |
|---|---|---|---|---|---|
| | | | | | |

**Estado (2026-08-31)**: plataforma/mecanismo de despliegue aún no decidido — pendiente de definir antes del primer deploy real, junto con el stack.

## Checklist de pre-deploy

[Completar: qué debe verificarse antes de cada deploy — ver agente `deployment-checklist`, que solo verifica, nunca ejecuta el deploy.]
