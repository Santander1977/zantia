# ESTADO ACTUAL — ZANTIA

> Snapshot. Se sobrescribe, no se acumula — la historia completa vive en `docs/changelog/`. Este archivo responde: "si entro ahora mismo, ¿qué necesito saber para no romper nada ni repetir trabajo?"

**Última actualización**: 2026-09-01 (gestión "en nombre de otro paciente" — beneficiario — recado 013)

## Qué está desplegado

| Componente | Versión/commit | Entorno | Estado |
|---|---|---|---|
| — | — | — | Nada desplegado todavía. `Dockerfile` + `service/app.py` (FastAPI/uvicorn) listos y probados SIN Docker (no hay runtime de contenedores en esta máquina) contra un `uvicorn` real + `curl` real. 120 tests pasando + 1 deshabilitado a propósito. Sin remoto git configurado — bloquea crear el servicio en EasyPanel hasta que el usuario haga push a un repo real. |

## Trabajo en curso sin commitear

Ver `git status` al momento de leer esto. Ver `/Users/enzoalfonso/recado/` (recados de esta sesión, numerados) para el detalle completo de cada fase.

## Conocido roto / pendiente de verificación

- Canal exacto de mensajería (WhatsApp/Telegram/etc.) sin decidir — `channels/contract.py` define la interfaz; `MockChannel` demuestra el contrato, sin implementación real.
- Jurisdicción/marco legal exacto para la regla de habeas data sin confirmar (ver `.claude/rules/proteccion-datos-personales.md`).
- `domains/{emergency,sales,citizen}/` son solo contratos — `domains/health/` ya es real, con adaptador real a hrmm-backend construido (ver `docs/health-demand-agent.md`).
- `core/brain.py:AnthropicBrain` es código real pero no probado en vivo (sin `ANTHROPIC_API_KEY` en esta sesión) — los tests usan `FakeBrain`/`HealthBrain` (ambos deterministas).
- Protocolo clínico de riesgo/urgencia real: sigue PENDIENTE DE VALIDACIÓN CLÍNICA/LEGAL (heredado de 003/004) — `domains/health/` reutiliza el mecanismo genérico no-clínico del Core, no un criterio médico real.
- Mecanismo de acceso/eliminación de datos personales por parte del usuario final (exigido por `.claude/rules/proteccion-datos-personales.md`): no implementado, `memory/user_memory.py` es deliberadamente parcial.
- **`HrmmAppointmentService` — solo lectura probada contra red real** (`GET /disponibilidad`, recados 010/011: `estado` confirmado real `"Libre"`/`"Reservado"`); **escritura (reservar/reprogramar/cancelar) NUNCA se ejecutó contra la red real** — no hay `HRMM_BACKEND_SECRET` disponible en esta máquina, y no existe entorno de staging documentado para hrmm-backend (confirmado). Antes de cualquier prueba de escritura real: decidir si se prueba contra producción con datos claramente marcados, con backup/verificación previos.
- No hay scheduler real que dispare recordatorios a su hora — `fire_reminder` se invoca explícitamente en este MVP.
- **Brecha de identidad teléfono↔documento — resuelta a nivel de diseño, extendida a beneficiarios** (recados 012/013, `.ai/RISKS.md` R-15): `domains/health/gateway.py:_gestionar_identificacion` pide el documento en el primer turno de un canal en `_CANALES_SIN_IDENTIFICADOR_DOCUMENTO` (hoy solo `"chatwoot"`), lo confirma contra `buscar-paciente`, y lo asocia para el resto de la conversación; `domains/health/brain.py` (013, primera vez tocado en el proyecto) reconoce además "es para mi mamá" y usa el documento del beneficiario (validado y confirmado) para la reserva, con auditoría separada gestor/beneficiario en `EventLog`. Sigue PENDIENTE: probarlo contra la red real / `HRMM_BACKEND_ENV=production` de verdad; un paciente genuinamente nuevo (titular o beneficiario) sería rechazado por `buscar-paciente`; reprogramar/cancelar con beneficiario (solo cubre reserva); recordar la relación entre conversaciones (mejora opcional, no implementada — requiere nueva capa de persistencia cruzando conversaciones).
- **Docker nunca se construyó ni se ejecutó realmente** (R-16) — no hay ningún runtime de contenedores instalado en esta máquina; se verificó el equivalente funcional (`uvicorn` real + `curl` real) pero no el build de la imagen.
- **Repo sin remoto git** (R-17) — bloquea crear el servicio en EasyPanel hasta que el usuario haga push a un repo real (GitHub u otro).
- **`ChatwootChannel` nunca se conectó a un inbox real** (R-4) — sin credenciales/acceso a Chatwoot en esta sesión; probado con fixtures y con un servidor `uvicorn` real vía `curl`.
- **`POST /webhook/chatwoot` sin autenticación de origen** (R-18) — Chatwoot no firma sus webhooks por defecto; mitigación pendiente antes de producción real.

## Progreso reciente

Resumen breve de las últimas sesiones relevantes. El detalle completo, sesión por sesión, vive en `docs/changelog/` (rotado por período — ver `.claude/rules/documentacion-y-memoria.md`).

- 2026-08-31: Proyecto creado desde `PROJECT-TEMPLATE` (commit `6fd36c7`) vía `/new-project`. Tipo: agente conversacional. Topología: monorepo único. Regla de dominio adicional: habeas data. Aislamiento verificado sin resultados.
- 2026-08-31 a 2026-09-01: Serie de auditorías de solo lectura (recados 001-005) sobre Dani (agente de ventas de referencia) y diseño conceptual de arquitectura para ICACO/ZANTIA — sin tocar código.
- 2026-09-01 (fase 1): Construcción del Core ZANTIA MVP. `git init` + primer commit. Stack: Python 3.9 + pydantic + pytest + `sqlite3`. `ConversationState`, máquina de estados, `StateStore` con concurrencia optimista, `Orchestrator`, `GuardrailEngine` (3 reglas), `ToolRegistry` + 3 tools demo, `EventLog`, agente de demostración. 34 tests. Ver recado `006-construccion-zantia.md`.
- 2026-09-01 (fase 2): Primer agente de dominio real sobre el Core existente — `domains/health/` (demanda inducida y gestión de citas para una IPS). Activity con 3 capas de estado separadas (D-3), AppointmentService/ActivitySource/ActivityResultSink (Mock), ReminderManager determinista (72h/24h/8h), HealthBrain. 2 defectos reales del Core corregidos y documentados. 29 tests nuevos (63/63). Ver recado `007-agente-demanda-inducida-zantia.md`.
- 2026-09-01 (fase 3): Evolución bidireccional — `domains/health/gateway.py` (correlación inbound/outbound, `PatientRequest`, clasificación de intención, cierre determinista de Activities sintéticas). 8 tests nuevos (71/71). Ver recado `008-acceso-bidireccional-zantia-health.md`.
- 2026-09-01 (fase 4): `HrmmAppointmentService` — adaptador real contra la API de agenda de hrmm-backend (verificada leyendo su código fuente real, solo lectura). Identidad por documento, espejo de catálogo (servicios/médicos), idempotencia de reserva reforzada, y sub-flujo de verificación por código de 6 dígitos (obligatorio de hrmm-backend para cancelar/reprogramar, descubierto durante esta fase — no estaba en el pedido original) construido enteramente en `gateway.py`, sin tocar `HealthBrain`. 20 tests nuevos (91 pasando + 1 deshabilitado a propósito). Sin pruebas de red real (decisión explícita del usuario). Ver recado `009-adaptador-real-hrmm-backend.md`.
- 2026-09-01 (fase 5): Validación real de solo lectura del mapeo de `estado` — `GET /disponibilidad` real confirmó `"Libre"`/`"Reservado"` como únicos valores; filtro de `get_availability` corregido de denylist-por-substring a allowlist explícito. `Cita.estado` sigue INFERENCIA (endpoint exige secreto no usado). 1 test nuevo (92 pasando + 1 deshabilitado). Ver recado `010-validacion-real-estado-hrmm.md`.
- 2026-09-01 (fase 6, recado `011`): Capa de configuración `domains/health/config.py` — único punto que decide Mock vs. `HrmmAppointmentService` según `HRMM_BACKEND_ENV` (default seguro `mock`; `production` exige URL+secreto o falla al arrancar; `staging` falla explícito, no existe). 7 tests nuevos (99 pasando + 1 deshabilitado).
- 2026-09-01 (fase 7, mismo recado `011-config-empaquetado-chatwoot-despliegue.md`): Empaquetado como servicio HTTP (`service/app.py`, FastAPI+uvicorn — decisión D-5) + canal real `ChatwootChannel` (`channels/chatwoot_channel.py`) + `Dockerfile`. Verificado con `uvicorn` real + `curl` real (Docker no disponible en esta máquina, R-16) — 1 bug real encontrado y corregido (fallo al responder a Chatwoot producía 500 sin contexto). Brecha de identidad teléfono↔documento identificada y documentada, no resuelta (R-15). Repo sin remoto git (R-17), sin inbox de Chatwoot real conectado (R-4). 10 tests nuevos (109 pasando + 1 deshabilitado).
- 2026-09-01 (fase 8, recado `012-gate-identidad-chatwoot.md`): Resuelta a nivel de diseño la brecha de identidad teléfono↔documento (R-15) — `domains/health/gateway.py:_gestionar_identificacion` pide el documento en el primer turno de un canal sin identificador-documento (`_CANALES_SIN_IDENTIFICADOR_DOCUMENTO = {"chatwoot"}`), lo confirma contra `buscar-paciente`, lo asocia en `_identidad_resuelta` para el resto de la conversación, y escala tras 3 intentos fallidos (mecanismo de escalamiento ya existente, `RequestStatus.ESCALADA`). Camino Activity (outbound) confirmado sin cambios. 6 tests nuevos (115 pasando + 1 deshabilitado), 0 de los 109 tests previos modificados. Sin tocar HealthBrain, ChatwootChannel ni HrmmAppointmentService. Abierto: nunca probado contra red real; pacientes genuinamente nuevos (sin historial en hrmm-backend) serían rechazados por `buscar-paciente` — decisión de producto sin resolver.
- 2026-09-01 (fase 9, recado `013-gestion-beneficiario.md`): Gestión "en nombre de otro paciente" — `domains/health/brain.py` (primera vez tocado en este proyecto, autorizado explícitamente) reconoce "es para mi mamá"/similares, pide y valida el documento del beneficiario contra `buscar-paciente`, exige confirmación explícita del nombre, y usa ese documento (nunca el del titular) en la reserva real. `agent.py` (extensión mínima) registra `GESTION_EN_NOMBRE_DE_BENEFICIARIO` en `EventLog` con `gestor_documento`/`beneficiario_documento` separados. Bug real encontrado y corregido: `interpret()` lowercaseaba el documento del beneficiario antes de validarlo. 5 tests nuevos (120 pasando + 1 deshabilitado), 0 de los 115 tests previos modificados. Alcance no cubierto: reprogramar/cancelar con beneficiario, y la mejora opcional de recordar la relación entre conversaciones (declarada PENDIENTE, esfuerzo no bajo).
