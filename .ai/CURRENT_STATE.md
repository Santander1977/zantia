# ESTADO ACTUAL — ZANTIA

> Snapshot. Se sobrescribe, no se acumula — la historia completa vive en `docs/changelog/`. Este archivo responde: "si entro ahora mismo, ¿qué necesito saber para no romper nada ni repetir trabajo?"

**Última actualización**: 2026-09-06 (ventana de gracia tras cierre de conversación, recado 050 — ver nota de brecha abajo)

> **Brecha de esta snapshot**: esta actualización corrige lo más urgente (el estado de R-6, abajo, estaba desactualizado) y agrega las entradas de los recados `049`/`050`, pero **no resume los recados `027` a `048`** (esta sesión no hizo ese trabajo, no tiene contexto de primera mano para resumirlo sin inventar). `git log` confirma que ese trabajo SÍ se commiteó y pusheó (`HEAD` de `main` = `f040c9f`, idéntico a `origin/main` — incluye al menos los recados 047/048, "interrupciones de contexto en cualquier etapa" y "saludo sin intención"). Quien retome este proyecto debería leer `/Users/enzoalfonso/recado/027-*.md` en adelante y completar esta sección — no asumir que el salto de `026` a `049` en "Progreso reciente" significa que no pasó nada en el medio.

## Qué está desplegado

| Componente | Versión/commit | Entorno | Estado |
|---|---|---|---|
| — | `85cf0ed` (main, `origin/main` confirmado idéntico) | Ninguno todavía (no hay servicio creado en EasyPanel, R-12 sigue ABIERTO) | Nada corriendo en producción real. `Dockerfile`/`service/app.py` verificados con **Docker real** (build + run, R-16 RESUELTO) en esta máquina, incluyendo una llamada real exitosa a hrmm-backend (`HRMM_BACKEND_SECRET` validado con `GET /api/agenda/citas` real, recado 025) y a la Bot API real de Telegram (recado 023). Remoto git existe y está al día (R-17 RESUELTO). 169 tests pasando + 2 deshabilitados a propósito. |

## Trabajo en curso sin commitear

Ver `git status` al momento de leer esto — no asumir que coincide con esta tabla. Al cerrar esta actualización (2026-09-06): `HEAD` de `main` (`f040c9f`) está al día con `origin/main` (confirmado con `git ls-remote`) — los recados hasta el `048` ya están commiteados y pusheados. Sin commitear: todo el trabajo del recado `050` (`core/orchestrator.py`, `domains/health/brain.py` — comentario, sin cambio de comportamiento —, `domains/health/gateway.py`, `tests/domains/health/test_ventana_de_gracia_tras_cierre.py`, nuevo), y además `registrar_webhook.sh`/`registrar_webhook.sh.save` y `.claude/rules/centro-de-control.md` (sin tocar por esta sesión, ya estaban así). Ver `/Users/enzoalfonso/recado/` (recados numerados) para el detalle completo de cada fase.

## Conocido roto / pendiente de verificación

- Jurisdicción/marco legal exacto para la regla de habeas data sin confirmar (R-2, `.claude/rules/proteccion-datos-personales.md`).
- Protocolo clínico de riesgo/urgencia real: sigue PENDIENTE DE VALIDACIÓN CLÍNICA/LEGAL (R-1, heredado de 003/004).
- Mecanismo de acceso/eliminación de datos personales: **parcialmente cubierto** desde recado 016 (eliminación de `identidad_canal` a pedido, "olvida mi información") — sigue sin cubrir `ConversationState`/`Activity`/`PatientRequest` ni ningún mecanismo de CONSULTA (R-3).
- **`HrmmAppointmentService` — lecturas Y escrituras reales confirmadas (recados 025, 049, 050) — R-6 → RESUELTO**: catálogo, `get_availability`, `get_patient_appointments` (lecturas, recado 025); reservar (`book_appointment`, al menos una cita real creada durante las pruebas del recado 049, `CITA-680ca01ca2` y otras con prefijo `SLOT-SEED...`) y cancelar (esa misma cita, cancelada de verdad contra producción con el código de verificación real enviado por correo — confirmado con una consulta posterior, `estado: cancelada`) SÍ se ejecutaron contra la red real, fuera de esta sesión de Claude Code de icaco (ejecutado directo contra la API de `hrmm-backend`, no a través de un canal de ZANTIA todavía — reprogramar sigue sin probarse contra red real). No existe entorno de staging documentado para hrmm-backend (R-7, sigue abierto).
- No hay scheduler real que dispare recordatorios a su hora (R-10) — `fire_reminder` se invoca explícitamente en este MVP.
- `ConversationMemory`/`EventLog` siguen en memoria de proceso, sin persistir (R-11) — a diferencia de `ConversationState`, que SÍ persiste desde recado 021 (ver abajo).
- Sin decisión/ejecución de plataforma de despliegue real — no hay servicio creado en EasyPanel todavía (R-12).
- `core/brain.py:AnthropicBrain` nunca probado en vivo (R-13) — todo corre con `FakeBrain`/`HealthBrain`, deterministas.
- **Brecha de identidad teléfono/chat↔documento — resuelta a nivel de diseño y extendida a Telegram** (R-15, recados 012/013/014/016/022): pide documento + código de verificación por correo antes de asociar, persiste en `identidad_canal` con vencimiento a 180 días y eliminación a pedido del paciente; ChatwootChannel Y TelegramChannel comparten el mismo mecanismo. Sigue abierto: nunca probado contra `HRMM_BACKEND_ENV=production` de verdad en una conversación completa; un paciente genuinamente nuevo sería rechazado por `buscar-paciente`; reprogramar/cancelar con beneficiario (solo cubre reserva).
- **Canal de mensajería real (R-4)**: decisión del usuario (2026-09-05) de usar SOLO Telegram por ahora. `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET` reales configurados y verificados con Docker real contra la Bot API real (recados 023/025) — falta registrar el webhook (`setWebhook`) contra una URL pública, que todavía no existe (nada desplegado). Chatwoot deliberadamente sin configurar — ya no es obligatorio para arrancar (D-8, recado 024).
- `POST /webhook/chatwoot` sin autenticación de origen (R-18) — riesgo de baja urgencia real mientras Chatwoot no esté configurado (responde 503).
- `HrmmAppointmentService.book_appointment` nunca envía `correo` al crear una cita (R-21, hallazgo de recado 015) — no corregido.
- `domains/{emergency,sales,citizen}/` siguen siendo solo contratos (R-14).

## Progreso reciente

Resumen breve de las últimas sesiones relevantes. El detalle completo, sesión por sesión, vive en `docs/changelog/` (rotado por período — ver `.claude/rules/documentacion-y-memoria.md`) y en los recados numerados de `/Users/enzoalfonso/recado/`.

- 2026-08-31: Proyecto creado desde `PROJECT-TEMPLATE` (commit `6fd36c7`) vía `/new-project`. Ver recados `001` a `005`.
- 2026-09-01 (fases 1-9, recados `006` a `013`): Core ZANTIA MVP, primer dominio real (`domains/health/`), evolución bidireccional, `HrmmAppointmentService` real, validación real de `estado`, empaquetado FastAPI+Chatwoot, gate de identidad teléfono↔documento (R-15), gestión "en nombre de otro paciente". 120 tests pasando + 1 deshabilitado a propósito al cierre de esta serie.
- 2026-09-02 (recado `014`): Identidad de canal PERSISTENTE entre conversaciones — tabla `identidad_canal` (`domains/health/identity_store.py`, store SQLite propio, decisión D-6), verificación por código de 6 dígitos antes de persistir (`POST /api/agenda/verificacion/confirmar`, endpoint PROPUESTO en hrmm-backend en ese momento). 130 tests pasando + 1 deshabilitado.
- 2026-09-02 (recado `015`): R-19 confirmado RESUELTO de forma independiente (`openapi.json` real de hrmm-backend en producción). Prueba real de extremo a extremo de `identidad_canal` preparada (manual/interactiva, `ZANTIA_RUN_REAL_HRMM_TESTS`), sin ejecutar — bloqueada por falta de `.env` real en ese momento y por depender de un humano leyendo un correo real. Hallazgo: `book_appointment` nunca envía `correo` (R-21).
- 2026-09-03 (recado `016`): Retención de `identidad_canal` — vencimiento automático a 180 días (`RETENCION_IDENTIDAD_DIAS`) + eliminación real a pedido del paciente ("olvida mi información", `HealthBrain`, con confirmación obligatoria). Bug real encontrado y corregido: una transición de fase inválida disparaba una escalación falsa. R-20 → MITIGADO. 142 tests pasando + 2 deshabilitados.
- 2026-09-04: Docker instalado (`brew install --cask docker`) y build/run reales confirmados por primera vez — R-16 → RESUELTO. Remoto git creado y push confirmado — R-17 → RESUELTO. `core/agent_contract.py:build_orchestrator` conectado a `ZANTIA_DB_PATH` real (antes hardcodeado a `:memory:`, hallazgo documentado como R-22 → RESUELTO); `SQLiteStateStore` ahora crea el directorio padre si falta. Ver recado `021`.
- 2026-09-05 (recado `022`): `TelegramChannel` (`channels/telegram_channel.py`) — segundo canal real, por webhook (decisión D-7, no polling). `"telegram"` agregado a `_CANALES_SIN_IDENTIFICADOR_DOCUMENTO` — comparte el mismo gate de identidad que Chatwoot sin código nuevo en el gate en sí.
- 2026-09-05 (recado `023`): Cierre de R-23 — `TelegramChannel.verificar_secreto_webhook` valida `X-Telegram-Bot-Api-Secret-Token` contra `TELEGRAM_WEBHOOK_SECRET` (fail-fast al arrancar) antes de procesar cualquier payload. Verificado con Docker real + la Bot API real de Telegram (llamada real sin efecto, "chat not found" para un chat_id de prueba).
- 2026-09-05 (recado `024`): Chatwoot pasa a ser OPCIONAL al arrancar (decisión D-8) — decisión del usuario de usar solo Telegram por ahora. Sin `CHATWOOT_URL`/`CHATWOOT_ACCOUNT_ID`, el proceso arranca igual (warning explícito), `POST /webhook/chatwoot` responde `503`.
- 2026-09-05 (recado `025`): Build/run real de Docker con credenciales reales de hrmm-backend + Telegram, sin Chatwoot. `HRMM_BACKEND_SECRET` confirmado VÁLIDO con una llamada real a un endpoint que sí lo exige (`GET /api/agenda/citas`). Hallazgo operativo: `HRMM_BACKEND_ENV=` (presente pero vacía) revienta el arranque — documentado en `.env.example`.
- 2026-09-05: Commit `85cf0ed` (todo el trabajo de Telegram/D-7/D-8) pusheado a `origin/main`, confirmado con `git fetch` — sin divergencia. Instalada la regla `.claude/rules/centro-de-control.md` (modo de solo lectura/reporte del estado del proyecto). 169 tests pasando + 2 deshabilitados a propósito al cierre de esta actualización.
- 2026-09-05 (recado `026`): **Primer bug real reportado desde una conversación real de un paciente por Telegram.** `domains/health/brain.py:_ACEPTA`/`_DECLINA` no reconocían una respuesta de una sola palabra sin tilde/coma ("Si", "No") como el mensaje completo — el paciente real quedó atascado repitiendo la misma pregunta de sí/no sin poder avanzar nunca. Corregido con comparación por igualdad (`_es_afirmativo`/`_es_negativo`), sin tocar el matching por substring existente (evita reintroducir falsos positivos). Hallazgo relacionado: los mensajes de "sin disponibilidad" contenían la frase prohibida "te contactamos" (`guardrails/rules.py`), que `NoPrometerContactoGuardrail` reescribía en silencio por un mensaje de escalamiento fantasma (sin escalar nada de verdad) — coincide con lo que el paciente real reportó haber recibido al pedir una cita; reformulados. R-24 (nueva) → RESUELTO. **No se pudo confirmar contra logs reales cuál fue el disparador exacto de ese segundo hallazgo** (R-11, `EventLog`/`ConversationMemory` en memoria sin persistir, sigue ABIERTO — este incidente es evidencia real de por qué importa). 174 tests pasando + 2 deshabilitados a propósito.
- **(brecha de esta snapshot: recados `027` a `048` no resumidos acá — ver nota al inicio del archivo y leer los recados directamente).** Confirmado por `git log`/`git ls-remote`: ese trabajo incluye al menos el recado `047` ("las 5 categorías de interrupción de contexto — `_PARA_OTRO`/`_INFO_NO_AUTORIZADA`/`_HUMANO`/`_NO_PUEDE_AHORA`/`_PIDE_INFO` — ahora se revisan en CUALQUIER etapa de una conversación abierta, no solo en la primera") y el `048` ("hola" sin intención ya no dispara el flujo de reserva en el mismo turno) — ambos commiteados y pusheados a `origin/main` (`f37e24d`, `8d37606`, `9837724`, `f040c9f`).
- 2026-09-06 (recado `049`, diagnóstico): **Segundo bug real reportado desde una conversación real por Telegram** — el fix del recado 047 NO cubrió el caso "conversación recién cerrada + mensaje siguiente sobre un beneficiario", porque ese detector solo vive dentro de `HealthBrain.interpret()`, inalcanzable sin conversación abierta (`gateway.py:_enrutar_solicitud_nueva`, el camino que se usa apenas se cierra una Activity, nunca lo conoció). Confirmado con evidencia real: la cita de Odontología (`CITA-680ca01ca2`, 2026-09-08 08:00, documento del titular) quedó reservada a nombre del titular en vez de la hija. Diagnóstico únicamente en este recado (pedido explícito del usuario), sin corregir todavía.
- 2026-09-06 (recado `050`, corrección): Ventana de gracia de UN turno tras cerrar una Activity (`gateway.py:_recien_cerrada`/`_evaluar_ventana_de_gracia`, nueva propiedad `Orchestrator.brain`) — el mensaje inmediatamente posterior a un cierre se reevalúa contra el mismo detector del recado 047 antes de tratarse como solicitud nueva. Decisión de diseño explícita para el caso más delicado (beneficiario declarado sobre una reserva YA ejecutada contra producción): nunca se cancela/reasigna nada automáticamente, se informa y se remite al flujo de cancelación ya existente y protegido por código de verificación. Dos bugs reales encontrados y corregidos en el camino: mismatch de clave en `_cerrar_si_definitivo` (documento vs. identificador de canal) y `core/orchestrator.py:_reabrir_ciclo` borrando `datos_recopilados` al reabrir un wizard desde `CIERRE`. 4 tests nuevos, **354 passed + 4 deshabilitados a propósito** (350 previas + 4 nuevas), cero regresiones. Sin commitear todavía (ver "Trabajo en curso sin commitear" arriba).
