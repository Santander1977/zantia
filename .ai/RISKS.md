# REGISTRO DE RIESGOS — ZANTIA

> Registro vivo — se actualiza cuando se detecta un riesgo nuevo o cuando uno existente se mitiga. No se archiva en `docs/changelog/` hasta que esté resuelto.

> Poblado el 2026-09-01 consolidando lo encontrado a lo largo de las fases de construcción (recados 006-009 en `/Users/enzoalfonso/recado/`). CRÍTICA/ALTA = bloquea operar con usuarios/datos reales. MEDIA/BAJA = no bloquea el MVP de demostración, sí bloquea producción real.

| ID | Riesgo | Impacto | Probabilidad | Componente | Evidencia | Mitigación | Prioridad | Estado |
|---|---|---|---|---|---|---|---|---|
| R-1 | Protocolo clínico de riesgo/urgencia sigue siendo el genérico no-médico heredado del Core, sin validar por personal clínico/legal | El sistema podría no detectar una urgencia real, o generar falsos positivos, en un dominio de salud | Alta si se despliega sin resolver | `core/brain.py:RISK_KEYWORDS_DEMO`, `domains/health/` | `003` sec. 19, `004`, recado `007` | Validar con criterio clínico/legal antes de cualquier uso con pacientes reales — no es una decisión de software | CRÍTICA | ABIERTO |
| R-2 | Jurisdicción/marco legal de protección de datos personales sin confirmar con el cliente | No se puede operar legalmente con datos reales de usuarios finales sin esto | Cierta (nunca se confirmó) | Proyecto completo | `.claude/rules/proteccion-datos-personales.md` | Confirmar explícitamente con el cliente/usuario el marco legal aplicable (ej. Ley 1581/2012, u otro) — nunca asumirlo | CRÍTICA | ABIERTO |
| R-3 | Mecanismo de acceso/eliminación de datos personales del usuario final no implementado | Incumple la regla obligatoria propia del proyecto (habeas data) | Cierta | `memory/user_memory.py` (deliberadamente parcial) | `.claude/rules/proteccion-datos-personales.md`, `.ai/CURRENT_STATE.md` | Diseñar e implementar antes de manejar datos reales de un usuario final real. **Parcialmente cubierto para `identidad_canal`** desde recado 016 ("olvida mi información", ver R-20) — sigue sin cubrir `ConversationState`/`Activity`/`PatientRequest` ni ningún mecanismo de CONSULTA ("qué datos tienes de mí"), solo eliminación de la identidad de canal | CRÍTICA | ABIERTO |
| R-4 | Sin canal de mensajería real CONECTADO — `ChatwootChannel` Y `TelegramChannel` (recado 022) ya construidos y probados con fixtures, ninguno conectado a un inbox/bot real todavía | El sistema no puede recibir/enviar mensajes de pacientes reales todavía, por ningún canal | Cierta hasta que se conecte al menos un canal real | `channels/chatwoot_channel.py`, `channels/telegram_channel.py`, `service/app.py` | Recado `011`, `022`, `023`, `024` | **Decisión del usuario (2026-09-05)**: por ahora solo se usará Telegram — Chatwoot (WhatsApp/Instagram) queda deliberadamente sin configurar, y desde recado 024 (D-8) ya no es obligatorio para que el proceso arranque. Telegram: verificado con Docker real (recado 023) que el proceso arranca con `TELEGRAM_BOT_TOKEN`/`TELEGRAM_WEBHOOK_SECRET` reales y responde correctamente contra la Bot API real (llamada real, sin efecto — "chat not found" para un chat_id de prueba); falta registrar el webhook (`setWebhook`) contra la URL pública de ZANTIA una vez desplegada. Chatwoot: sigue pendiente si el usuario decide activarlo más adelante — el usuario crearía un inbox de PRUEBA y configuraría `CHATWOOT_URL`/`CHATWOOT_ACCOUNT_ID`/`CHATWOOT_API_TOKEN` | ALTA | PARCIALMENTE MITIGADO |
| R-5 | Capa de configuración Mock/Hrmm según `HRMM_BACKEND_ENV` | — | — | `domains/health/config.py` | Recado `011` | Construida y probada (7 tests: default mock, mock explícito, production completo, production sin URL, production sin secreto, staging, valor desconocido) | ALTA | MITIGADO |
| R-6 | `HrmmAppointmentService` nunca se ejecutó contra la red real de hrmm-backend | Puede fallar de formas no anticipadas (formatos, límites de tasa, errores no mapeados) contra el sistema real | Media (depende de qué tan fiel fue la lectura de código fuente) | `domains/health/hrmm_appointment_service.py` | Recado `009` | Ejecutar el test de integración real ya escrito pero deshabilitado (`ZANTIA_RUN_REAL_HRMM_TESTS`) contra un entorno confirmado con el usuario | ALTA | ABIERTO |
| R-7 | No existe entorno de staging para hrmm-backend | Cualquier prueba de integración real expondría datos/acciones contra producción | Cierta (confirmado por investigación de código) | hrmm-backend (proyecto externo) | Recado `009` | Decidir con el usuario: ¿producción con datos claramente marcados + backup/verificación previos, o construir un staging? | ALTA | ABIERTO |
| R-8 | Sin manejo de reintentos/backoff ni de límites de tasa (429) hacia hrmm-backend | Una llamada real podría fallar sin recuperación; hrmm-backend confirmó límites reales (3/300s en verificación, 5/60s en reserva/confirmación) | Media, sube con el volumen de uso | `domains/health/hrmm_http.py:RealHttpClient` | Investigación de `hrmm-backend/app/api/agenda.py`, recado `009` | Implementar reintento con backoff exponencial y manejo explícito de 429 antes de producción | MEDIA | ABIERTO |
| R-9 | Mapeo de `Cita.estado` (`_MAPA_ESTADO_HRMM`) sigue siendo INFERENCIA no confirmada — el endpoint que lo expone (`GET /api/agenda/citas`) exige `X-Backend-Secret`, no usado en esta sesión. **Parcialmente resuelto**: `BloqueDisponibilidad.estado` (campo distinto, usado por `get_availability`) SÍ se validó con una llamada GET real y solo-lectura contra producción — únicos valores reales: `"Libre"`/`"Reservado"` — filtro corregido a allowlist explícito | Una cita (no un bloque de disponibilidad) podría clasificarse con el `AppointmentStatus` incorrecto | Media (solo para `Cita.estado`; resuelto para disponibilidad) | `hrmm_appointment_service.py:_MAPA_ESTADO_HRMM` | Recado `009`, `010` | Validar `_MAPA_ESTADO_HRMM` con una llamada real autorizada (requiere `HRMM_BACKEND_SECRET`) antes de confiar en esta lógica para decisiones críticas | MEDIA | PARCIALMENTE MITIGADO |
| R-10 | Sin scheduler real que dispare recordatorios (72h/24h/8h) por sí solo | En producción, nadie invocaría `fire_reminder` automáticamente a la hora programada | Cierta | `domains/health/reminder_manager.py` + capa de disparo (no construida) | Recado `007`, `docs/health-demand-agent.md` | Decidir mecanismo (cron, cola, worker) y construirlo | ALTA | ABIERTO |
| R-11 | Persistencia solo parcial: `ConversationState` en SQLite real (desde recado 021 — antes ni siquiera esto, ver R-22), pero `ConversationMemory` y `EventLog` siguen viviendo únicamente en memoria de proceso | Pérdida de historial conversacional (turnos recientes que ve el Brain) y de auditoría (`EventLog`) ante un reinicio, o inconsistencia con múltiples instancias — el `ConversationState` en sí ya sobrevive, pero el Brain perdería el contexto reciente de la conversación y `EventLog` perdería la traza de eventos previos | Cierta si se despliega tal cual, sin resolver | `memory/conversation_memory.py`, `observability/events.py` | `.ai/ARCHITECTURE.md`, `.ai/TESTING.md`, recado `021` (acotó el alcance: confirmó que esto es lo que queda después de resolver R-22) | Decidir mecanismo de persistencia real para `ConversationMemory`/`EventLog` antes de un despliegue con reinicios o múltiples instancias — mismo patrón que `StateStore`/`ZANTIA_DB_PATH` es un candidato razonable, no decidido todavía | MEDIA | ABIERTO |
| R-12 | Sin decisión de plataforma/stack de despliegue | No se puede desplegar sin esto | Cierta | Proyecto completo | `.ai/DEPLOYMENT.md`, `PROJECT.md` | Decidir plataforma (EasyPanel es el precedente de otros proyectos de este ecosistema, pero no se asume) | ALTA | ABIERTO |
| R-13 | `AnthropicBrain` (Core) nunca probado en vivo — todo el sistema corre hoy con `FakeBrain`/`HealthBrain`, deterministas por palabras clave, no NLU real | El sistema no entiende lenguaje natural variado en producción todavía | Cierta si se espera NLU real | `core/brain.py:AnthropicBrain` | Recado `006` | Configurar `ANTHROPIC_API_KEY` y probar, o diseñar un Brain real basado en LLM para el dominio salud (trabajo nuevo, no solo activar el existente) | MEDIA | ABIERTO |
| R-14 | `domains/{emergency,sales,citizen}/` sin implementar — solo contratos | ZANTIA como "plataforma multi-dominio" hoy solo tiene un dominio real (salud) | Cierta | `domains/` | `.ai/ARCHITECTURE.md` | Fuera de alcance hasta que se decida atacar un dominio nuevo — no bloquea que el dominio salud funcione | BAJA | ABIERTO |
| R-15 | Brecha de identidad teléfono↔documento: `ChatwootChannel` entrega el número de WhatsApp como `patient_reference`, pero `HrmmAppointmentService` lo trata como si fuera el documento real del paciente (convención D-4) | Una reserva REAL iniciada por WhatsApp con `HRMM_BACKEND_ENV=production` fallaría o guardaría un documento incorrecto | Baja — mecanismo construido y probado (6+5+9 tests, `FakeHttpClient`), pero nunca ejercido contra la red real ni con `HRMM_BACKEND_ENV=production` de verdad | `service/app.py`, `domains/health/gateway.py:_gestionar_identificacion`, `domains/health/brain.py` (extensión de beneficiario, recado 013), `domains/health/identity_store.py` (persistencia, recado 014) | Recados `012`, `013`, `014` | **Resuelto para el diseño**: se pide el documento explícitamente en el primer turno de un canal sin identificador-documento (`_CANALES_SIN_IDENTIFICADOR_DOCUMENTO = {"chatwoot"}`), se confirma contra `buscar-paciente`, se asocia a `_identidad_resuelta` para el resto de la conversación, con escalamiento tras 3 intentos fallidos. **Extendido en 013**: además de la identidad del titular, `HealthBrain` reconoce gestión "en nombre de otro paciente" ("es para mi mamá") — pide y valida el documento del BENEFICIARIO contra `buscar-paciente`, exige confirmación explícita, y usa ese documento (nunca el del titular) para reserva/disponibilidad — con auditoría separada en `EventLog` (`gestor_documento` vs. `beneficiario_documento`, nunca fusionados). **Extendido en 014**: la identidad del titular ya no se acepta solo con `buscar-paciente` — se exige además un código de verificación por correo (mismo mecanismo de 009) antes de asociarla, y el resultado se persiste en la tabla `identidad_canal` (`domains/health/identity_store.py`), INDEPENDIENTE de cualquier conversación puntual — un contacto siguiente del mismo teléfono se reconoce de inmediato, sin repetir el wizard, incluso tras un reinicio del proceso (si `ZANTIA_IDENTIDAD_DB_PATH` apunta a un archivo real). **Abierto**: (a) `buscar-paciente` solo encuentra pacientes con historial previo en hrmm-backend — un paciente (titular o beneficiario) genuinamente nuevo sería rechazado aunque su documento sea válido (decisión de producto no resuelta); (b) nunca probado contra la red real ni con `HRMM_BACKEND_ENV=production` de verdad; (c) la gestión para beneficiario solo cubre el flujo de RESERVA vía `HealthBrain` — reprogramar/cancelar vía el wizard de verificación de `gateway.py` (Hrmm) todavía no distingue beneficiario, sigue usando la identidad ya resuelta del titular; (d) la mejora opcional de "recordar la relación" (ej. etiqueta "mamá") no se implementó; (e) el paso de código de 014 depende de un endpoint de hrmm-backend que todavía no existe — ver R-19 (bloqueante, distinto de "no probado contra red real": aquí ni siquiera hay un endpoint real que probar); (f) sin política de retención para `identidad_canal` — RESUELTO en recado 016, ver R-20. **Extendido en recado 022**: `TelegramChannel` agregado a `_CANALES_SIN_IDENTIFICADOR_DOCUMENTO` — un `chat.id` de Telegram pasa por el MISMO mecanismo completo (wizard, verificación por código, `identidad_canal`, vencimiento a 180 días, olvido a pedido), comparte la misma tabla que WhatsApp/Chatwoot, sin ningún código nuevo en el gate en sí | ALTA | PARCIALMENTE MITIGADO |
| R-19 | ~~`HrmmAppointmentService.confirm_verification_code` (recado 014) llama a `POST /api/agenda/verificacion/confirmar` — endpoint PROPUESTO/coordinado con el usuario, que no existía en hrmm-backend~~ | El paso de verificación por código del gate de identidad de canal (014) no podía ejecutarse contra la red real hasta que este endpoint se implementara del lado de hrmm-backend | Era cierta | `domains/health/hrmm_appointment_service.py:confirm_verification_code`, `domains/health/gateway.py:_procesar_codigo_de_identificacion` | Recado `014` (hallazgo), Recado `015` (implementado en hrmm-backend, commit `4b928b2` en `feature/api-backend`, y confirmado independientemente el 2026-09-02: `GET https://curson8n-hrmm-backend.byrp3l.easypanel.host/openapi.json` en producción real lista `POST /api/agenda/verificacion/confirmar` — no solo declarado por el usuario, verificado con una llamada real de solo lectura) | **RESUELTO**: el endpoint ya existe y responde en producción real de hrmm-backend. Contrato del lado ZANTIA (`confirm_verification_code`) queda confirmado sin ajustes — coincide exactamente con lo PROPUESTO en recado 014. Sigue **PENDIENTE** una prueba de extremo a extremo con un código real recibido por correo (bloqueada por dos cosas distintas: (a) esta máquina/repo no tiene `.env` con `HRMM_BACKEND_SECRET`/`HRMM_BACKEND_URL` reales configurados todavía — ver R-6/R-7 — y (b) el código se envía por correo real, que ningún agente puede leer por sí mismo; requiere un humano revisando la bandeja de entrada). Test gateado ya preparado (`tests/domains/health/test_identidad_persistente.py::test_identidad_canal_real_e2e`, `ZANTIA_RUN_REAL_HRMM_TESTS`), sin ejecutar todavía — ver recado `015` | ALTA | RESUELTO (2026-09-02) — sub-pendiente de prueba E2E real, ver nota |
| R-20 | ~~Sin política de retención/vencimiento para la tabla `identidad_canal` (recado 014) — decisión explícita del usuario de no definirla en esa fase~~ | Un teléfono quedaba asociado a un documento indefinidamente, incluso si el número cambiaba de dueño (línea reciclada) o el paciente pedía que se olvide su dato | Era cierta | `domains/health/identity_store.py`, `domains/health/brain.py`, `domains/health/gateway.py` | Requisito #4 del pedido de recado 014: "No definas vencimiento en esta fase". Resuelto en recado 016 (2026-09-03) con decisiones de producto explícitas del usuario | **MITIGADO**: (1) **Vencimiento automático a 180 días** (`domains/health/identity_store.py:RETENCION_IDENTIDAD_DIAS`, constante nombrada única) — una fila `VERIFICADO` con `verificado_en` a más de 180 días de "ahora" (`IdentidadCanal.vigente()`) se trata como si no existiera al hidratar (`gateway.py`), sin ningún mensaje especial — el wizard completo de 012/014 se dispara de nuevo, igual que con un teléfono nuevo; re-verificar actualiza `verificado_en` (UPSERT, nunca duplica fila). (2) **Eliminación a pedido del paciente** — `HealthBrain` (`brain.py:_OLVIDAR`) detecta la intención en CUALQUIER etapa de una conversación abierta, exige confirmación explícita, y solo entonces `gateway.py:_procesar_olvido_si_corresponde` borra la fila REAL (`identity_store.eliminar`) y registra `IDENTIDAD_ELIMINADA_A_PEDIDO` en EventLog. 8 tests nuevos (`test_identidad_persistente.py` + `test_identidad_olvido.py`), suite completa 142/142 (+2 deshabilitados a propósito) | MEDIA | MITIGADO (2026-09-03) |
| R-21 | `HrmmAppointmentService.book_appointment` nunca envía el campo `correo` en el body de `POST /api/agenda/citas` — hallazgo encontrado al preparar `test_identidad_canal_real_e2e` (recado 015), no corregido (fuera del alcance del pedido de esa sesión) | Cualquier cita reservada exclusivamente vía ZANTIA (Chatwoot) queda SIN correo en hrmm-backend — `POST /api/agenda/verificacion/enviar` (009) no tendría a dónde mandar el código de verificación para ese paciente (`enviado=False`) si su única cita fue reservada por este canal. No bloquea el flujo de REGISTRO de identidad (014/015, que no depende de ninguna cita), pero sí el sub-flujo de reprogramar/cancelar por código (009) para un paciente cuya única cita viene de ZANTIA | Cierta si un paciente reserva su primera cita real por WhatsApp/Chatwoot y nunca antes tuvo una cita con correo por otro canal (portal, webchat) | `domains/health/hrmm_appointment_service.py:book_appointment` | Recado `015` (encontrado al leer `book_appointment` para diseñar la prueba real — nunca antes verificado con este detalle) | Agregar `correo` al `json_body` de `book_appointment` cuando `HealthBrain`/el flujo de reserva lo tenga disponible (hoy el Protocol `AppointmentService.book_appointment` no recibe un `correo` explícito — requeriría revisar la firma o `register_patient_contact`) — no implementado, requiere decisión de diseño, no una corrección trivial | MEDIA | ABIERTO |
| R-16 | ~~El `Dockerfile` de ZANTIA nunca se construyó ni se ejecutó con Docker real — no hay ningún runtime de contenedores (Docker/podman/nerdctl/colima) instalado en esta máquina~~ | El build podía fallar en EasyPanel de formas no anticipadas (versión de base image, capas, exclusiones de `.dockerignore`) | Era cierta | `Dockerfile` | Recado `011` (hallazgo), instalación de Docker Desktop (`brew install --cask docker`, 2026-09-04) y build/run reales el mismo día | **RESUELTO**: `docker build -t zantia:local-test .` completó sin errores (deps de `requirements.txt` instaladas, imagen exportada). `docker run` con variables mínimas de modo mock (`CHATWOOT_URL`, `CHATWOOT_ACCOUNT_ID`, `CHATWOOT_API_TOKEN`, `HRMM_BACKEND_ENV=mock`) arrancó limpio (`Uvicorn running on http://0.0.0.0:8000`) y `GET /health` respondió `{"status":"ok"}` HTTP 200 contra el contenedor real. Confirmado también que sin `CHATWOOT_URL`/`CHATWOOT_ACCOUNT_ID` el proceso falla rápido y explícito (`HealthConfigError`), no en silencio — mismo patrón que el resto del proyecto. Pendiente distinto, no cubierto acá: build/run reales específicamente en el entorno de EasyPanel (plataforma de destino), ver R-12 | ALTA | RESUELTO (2026-09-04) |
| R-17 | ~~El repositorio de ZANTIA no tenía remoto git configurado (`git remote -v` vacío)~~ | No era posible dar a EasyPanel un repo/rama reales para desplegar — EasyPanel necesita un remoto (GitHub/GitLab u otro). Mientras tanto, todo el historial (incluyendo recados 010-014) existía solo en esta máquina, sin respaldo real | Era cierta | Repo completo | Recado `011` (hallazgo), confirmado por `git remote -v`/`git push`/`git fetch` reales el 2026-09-02 | **RESUELTO**: se creó el remoto `origin` (`https://github.com/Santander1977/zantia.git`) y se hizo `git push -u origin main` — confirmado con `git fetch origin` + `git log --oneline -1` local y `git log origin/main --oneline -1`, ambos apuntando al mismo commit `6a9d68a` (el commit que consolidó recados 010-014). `main` quedó trackeando `origin/main` sin divergencia | ALTA | RESUELTO (2026-09-02) |
| R-18 | `POST /webhook/chatwoot` no autentica el origen de la petición (Chatwoot no firma sus webhooks por defecto) | Cualquiera que conozca la URL del servicio podría enviar un payload con forma válida y disparar el flujo del dominio salud | Baja mientras el inbox sea de prueba y `HRMM_BACKEND_ENV=mock`; MÁS baja todavía desde recado 024 (D-8) — Chatwoot ni siquiera está configurado en el despliegue actual (el usuario solo usa Telegram por ahora), así que este endpoint responde `503` sin exponer ninguna superficie real | `service/app.py:webhook_chatwoot` | Recado `011`, `024`, `.ai/API_CONTRACTS.md` | Restringir por IP de origen de Chatwoot, o activar un secreto de webhook si la instancia de Chatwoot del ecosistema lo soporta (no verificado) — antes de cualquier uso con datos reales, si el usuario decide activar Chatwoot más adelante | MEDIA | ABIERTO |
| R-23 | ~~`POST /webhook/telegram` no autentica el origen de la petición (recado 022) — mismo hueco que R-18, pero con una mitigación real DISPONIBLE y no usada: Telegram soporta un `secret_token` en `setWebhook`~~ | Cualquiera que conozca la URL del servicio podía enviar un payload con forma válida y disparar el flujo del dominio salud, igual que R-18 | Era cierta | `service/app.py:webhook_telegram`, `channels/telegram_channel.py` | Recado `022` (hallazgo), Recado `023` (resuelto) | **RESUELTO**: `TelegramChannel.verificar_secreto_webhook()` (`hmac.compare_digest`, mismo criterio que `hrmm-backend/app/trusted_auth.py`) valida el header `X-Telegram-Bot-Api-Secret-Token` contra `TELEGRAM_WEBHOOK_SECRET` ANTES de leer el body como JSON — sin coincidir, `service/app.py:webhook_telegram` responde `401` sin procesar nada. `TELEGRAM_WEBHOOK_SECRET` es obligatoria para que el proceso arranque (fail-fast, `_construir_canal_telegram`, mismo criterio que `CHATWOOT_URL`/`CHATWOOT_ACCOUNT_ID`) — nunca expone el endpoint sin poder validarlo. 8 tests nuevos (4 unitarios en `TelegramChannel`, 3 de integración en `service/app.py` incluido el fail-fast de arranque), suite completa 167/167 (+2 deshabilitados a propósito) | MEDIA | RESUELTO (2026-09-05) |
| R-22 | ~~`core/agent_contract.py:build_orchestrator` construía siempre `SQLiteStateStore(":memory:")` hardcodeado — `ZANTIA_DB_PATH` existía en `.env.example` y en `core/config.py:ZantiaConfig.db_path` desde el principio, pero nunca se conectaba en el camino real. Documentada primero como hallazgo lateral en recado 014 (bloqueó reutilizar el mismo store para `identidad_canal`, ver D-6) sin abrirse como riesgo propio en ese momento~~ | Cualquier conversación real (de cualquier dominio, no solo salud — el Core es agnóstico) perdía TODO su `ConversationState` (fase, datos recopilados, herramientas usadas) si el proceso se reiniciaba a mitad de camino — no un caso extremo: exactamente lo que pasa en cualquier deploy/restart real de producción | Era cierta — confirmado que era la única instanciación de un store en memoria hardcodeada en todo `core/` (`grep` sobre el árbol completo, sin `.venv`/`tests`) | `core/agent_contract.py:build_orchestrator`, `state/store.py:SQLiteStateStore` | Recado `014`/D-6 (hallazgo original), Recado `021` (resuelto) | **RESUELTO**: `build_orchestrator` ahora lee `DEFAULT_CONFIG.db_path` (`core/config.py`, ya existía, nunca se usaba) y construye `SQLiteStateStore(db_path)` con la ruta real si `ZANTIA_DB_PATH` está configurada. Sin la variable, cae a `":memory:"` pero ya NUNCA en silencio — `logger.warning` explícito (`zantia.core`) en cada conversación nueva. `SQLiteStateStore.__init__` ahora también crea el directorio padre si no existe (evita un crash críptico en el primer mensaje real si `ZANTIA_DB_PATH` apunta a una carpeta nueva). Aislamiento entre conversaciones distintas contra el mismo archivo confirmado por diseño (clave primaria `conversation_id`) y con test explícito de "tres procesos" contra el mismo archivo. 4 tests nuevos (`tests/core/test_agent_contract_persistencia.py`), suite completa 147/147 (+2 deshabilitados a propósito, sin cambios). D-6 revisada explícitamente (recado 021): con la brecha resuelta, la razón ORIGINAL para no reutilizar `ZANTIA_DB_PATH` para `identidad_canal` ya no aplica, pero la decisión de mantenerlos SEPARADOS se sostiene por una razón distinta y más fuerte — ciclos de vida y claves primarias incompatibles (`conversation_id` vs `telefono`, sin retención vs. retención de 180 días) — ver recado 021 para el detalle completo | ALTA | RESUELTO (2026-09-04) |
